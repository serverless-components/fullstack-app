app: fullstack
component: express@1.1.0
name: fullstack-api

inputs:
  src: ./
  roleName: ${output:fullstack-permissions.name}
  domain: api.${env:domain}
  inference: true
  env:
    db: ${output:fullstack-db.name}
    dbIndex1: ${output:fullstack-db.indexes.gsi1.name}
    tokenSecret: ${env:tokenSecret}